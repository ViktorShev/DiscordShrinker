{
  "name": "discordshrinker",
  "module": "src/index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "bun build --compile --minify --sourcemap=none src/index.ts --outfile dist/DiscordShrinker"
  },
  "dependencies": {
    "yocto-spinner": "^1.2.0"
  }
}
